const bufferSize = 30 * 1024 * 1024
const buffer = Buffer.allocUnsafe(bufferSize)

setInterval(test.intervalFn)
setInterval(() => {
    void buffer
    test.intervalFn()
})
setInterval(test.intervalFn, 1000)